% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MethQcSet-io.R
\name{check_platform}
\alias{check_platform}
\title{Detect Array Platform from IDAT Files and Annotate Metadata}
\usage{
check_platform(meta, max_retries = 5)
}
\arguments{
\item{meta}{A \code{data.frame} with required column \code{Basename} (path prefix to
IDAT files, e.g. \verb{/path/to/idats/Sentrix_ID}). A \code{Sample_Name} column is
used for messaging; if absent, row numbers are used.}

\item{max_retries}{Integer. Number of read attempts per sample before
marking as \code{"Failed"}. Default: 5.}
}
\value{
\code{meta} with four columns appended:
\describe{
\item{Platform}{Detected platform: \code{"EPIC"}, \code{"EPICv2"}, \code{"450K"}, \code{"27K"}, or \code{NA}}
\item{Array_Size}{Number of probes in the raw IDAT}
\item{Manifest}{Manifest string from array annotation (may be \code{NA})}
\item{Status}{\code{"Success"} or \code{"Failed"}}
}
}
\description{
Reads IDAT files for each sample in \code{meta} and detects the Illumina array
platform by reading probe count from the raw channel data. Appends
\code{Platform}, \code{Array_Size}, \code{Manifest}, and \code{Status} columns to \code{meta}.
}
\details{
Use this function before \code{runMethQC()} when the platform is unknown or when
you need to check whether a cohort spans multiple array types.

Platform is inferred from array size using the same logic as
\code{meth_check_platform.R::CheckPlatform()}:
\itemize{
\item \verb{> 1,100,000} probes → \code{"EPICv2"}
\item \verb{> 1,000,000} probes → \code{"EPIC"}
\item \verb{> 400,000} probes → \code{"450K"}
\item \verb{> 20,000} probes → \code{"27K"}
}

If the annotation string disagrees with the array size, the array-size
inference is used and a warning is issued.
}
\examples{
\dontrun{
meta <- read.table("meta.tsv", header = TRUE, sep = "\t")
meta_updated <- check_platform(meta)
table(meta_updated$Platform)

# Split by platform before running QC
meta_epic <- meta_updated[meta_updated$Platform == "EPIC", ]
qcset <- runMethQC(meta_epic)
}

}
