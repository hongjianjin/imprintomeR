% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MethQcSet-io.R
\name{runMethQC}
\alias{runMethQC}
\title{Load IDAT Files, Run Quality Control, and Create a MethQcSet}
\usage{
runMethQC(
  meta,
  platform = NA,
  pcutoff = 0.05,
  save_qc_report = TRUE,
  outdir = ".",
  prefix = NULL,
  ...
)
}
\arguments{
\item{meta}{A \code{data.frame} with columns:
\describe{
\item{Sample_Name}{Sample identifiers (required)}
\item{Basename}{IDAT file path prefix, e.g. \verb{/path/to/idats/Sentrix_ID}
(required). Files \code{Sentrix_ID_Red.idat} and \code{Sentrix_ID_Grn.idat}
must exist at that path.}
\item{Platform}{(optional) Platform string. Used by platform resolution
when \code{platform = NA}.}
}}

\item{platform}{Platform string (\code{"EPIC"}, \code{"EPICv2"}, \code{"450K"}) or \code{NA}
(default). Resolution rules:
\describe{
\item{\code{NA} + \code{Platform} column in meta with one unique value}{Auto-use
that value. Run \code{check_platform(meta)} first to populate this column.}
\item{\code{NA} + \code{Platform} column in meta with multiple values}{Error with
instructions to subset meta by platform and re-call.}
\item{\code{NA} + no \code{Platform} column in meta}{Error with instructions to
run \code{check_platform(meta)} first.}
\item{Explicit string (e.g. \code{"EPIC"})}{Use provided value; any existing
\code{Platform} column in meta is ignored.}
}}

\item{pcutoff}{Numeric. Maximum mean detection p-value to pass QC (default: 0.05).}

\item{save_qc_report}{Logical. If \code{TRUE}, write \code{minfi::qcReport()}
density-report PDFs for all, PASS, and FAIL samples. Default: \code{TRUE}.}

\item{outdir}{Directory for QC density-report PDFs. Default: \code{"."}.}

\item{prefix}{Filename prefix for QC density-report PDFs. If \code{NULL},
the resolved platform name is used.}

\item{...}{Additional arguments passed to \code{minfi::read.metharray.exp()}.}
}
\value{
A \code{MethQcSet} object with:
\itemize{
\item \code{beta} slot populated from \code{minfi::getBeta()}
\item \code{detection_pval} slot populated from \code{minfi::detectionP()}
\item \code{qc_tables} slot populated with QC metrics
\item \code{platform} slot set to the resolved platform string
}
}
\description{
Factory function that reads IDAT files for all samples in \code{meta}, extracts
beta values and detection p-values via minfi, constructs a \code{MethQcSet}
object, and computes QC metrics. Returns a fully populated \code{MethQcSet}.
}
\examples{
\dontrun{
# Case 1: Known single-platform cohort
meta <- read.table("meta.tsv", header = TRUE, sep = "\t")
qcset <- runMethQC(meta, platform = "EPIC")

# Default minfi density-report PDFs:
qcset <- runMethQC(meta, platform = "EPIC",
                   outdir = "qc_output",
                   prefix = "epic")

# Case 2: Platform unknown - detect first, then run QC
meta <- check_platform(meta)
table(meta$Platform)  # inspect

meta_epic <- meta[meta$Platform == "EPIC", ]
qcset_epic <- runMethQC(meta_epic)  # platform auto-resolved from meta$Platform

meta_450k <- meta[meta$Platform == "450K", ]
qcset_450k <- runMethQC(meta_450k)
}

}
\seealso{
\code{\link[=check_platform]{check_platform()}} to detect and annotate platforms before calling
this function; \linkS4class{MethQcSet} for the returned object.
}
