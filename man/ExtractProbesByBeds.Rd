% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_probeset.R
\name{ExtractProbesByBeds}
\alias{ExtractProbesByBeds}
\title{Extract Probes Overlapping Input BED Region(s)}
\usage{
ExtractProbesByBeds(
  bed = NULL,
  bedFile = NULL,
  probes.all = NULL,
  assay = NULL,
  version = "HG19",
  verbose = FALSE,
  file = NULL
)
}
\arguments{
\item{bed}{Character vector of BED regions (\code{chr:start-end}) or a 3-column
BED-like data frame. Used when \code{bedFile} is \code{NULL}.}

\item{bedFile}{Optional BED file path. When provided, this mode is used.}

\item{probes.all}{Optional annotation table. If \code{NULL}, loads
\code{inst/extdata/anno.uniq_harmonized.liftover.rds}.}

\item{assay}{Optional assay filter.}

\item{version}{Genome build label (\code{"HG19"} or \code{"HG38"}) used to select
annotation columns.}

\item{verbose}{Logical passed to \code{bedr} helpers.}

\item{file}{Optional output path for writing tab-delimited results.}
}
\value{
Data frame of matched probes. In \code{bedFile} mode, returns region-aware
output compatible with legacy \code{ExtractProbesByBeds2()}.
}
\description{
Unified implementation for probe extraction from either in-memory BED
coordinates (\code{bed}) or BED file input (\code{bedFile}). This harmonizes legacy
\code{ExtractProbesByBeds()} and \code{ExtractProbesByBeds2()} behavior.
}
