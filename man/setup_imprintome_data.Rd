% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{setup_imprintome_data}
\alias{setup_imprintome_data}
\title{Setup imprintomeR Data Files}
\usage{
setup_imprintome_data(force = FALSE, verbose = TRUE)
}
\arguments{
\item{force}{Logical. If TRUE, re-download files even if they exist.}

\item{verbose}{Logical. If TRUE, print download progress.}
}
\value{
Invisibly returns TRUE if successful, FALSE otherwise.
}
\description{
Downloads required annotation data files for the imprintomeR package.
This is automatically called during package load if files are missing.
}
\details{
The imprintomeR package requires two large annotation files:
\itemize{
\item \code{anno.uniq_harmonized.liftover.rds} (52 MB): CpG probe annotations and genomic coordinates
\item \code{probesets_hg19.rds} (256 KB): ICR probeset definitions
}

These files are stored externally and downloaded on demand to keep the
repository lean. They are cached in \verb{inst/extdata/} after download.
}
\examples{
\dontrun{
  # Automatically called on package load if files are missing
  setup_imprintome_data()

  # Force re-download
  setup_imprintome_data(force = TRUE)
}

}
