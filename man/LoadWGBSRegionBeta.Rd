% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_io.R
\name{LoadWGBSRegionBeta}
\alias{LoadWGBSRegionBeta}
\title{Load wgbstools Region-Level Beta Table}
\usage{
LoadWGBSRegionBeta(
  input,
  probeset = "Rosenski_region",
  genome = c("hg19", "hg38"),
  strict = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{input}{Data frame or path to a tab-delimited \code{wgbstools beta_to_table} output. Required columns are \code{chr}, \code{start}, and \code{end}; sample beta columns are expected after optional coordinate columns such as \code{startCpG} and \code{endCpG}.}

\item{probeset}{Optional probeset name or data frame used to order/filter regions. Defaults to \code{"Rosenski_region"}. Set to \code{NULL} to keep all input regions in file order.}

\item{genome}{Genome build used when loading a named probeset. Currently supports \code{"hg19"} and \code{"hg38"}. Ignored when \code{probeset} is \code{NULL} or a data frame.}

\item{strict}{Logical. If \code{TRUE}, require every probeset region to be present in the input table when \code{probeset} is used.}

\item{verbose}{Logical. Print input and matched dimensions.}
}
\value{
Numeric matrix with region IDs as row names and samples as columns. Region metadata are stored in \code{attr(x, "regions")}; matched probeset annotation is stored in \code{attr(x, "probeset")} when available.
}
\description{
Reads a \code{wgbstools beta_to_table} output table, converts each genomic interval to a stable region ID (\code{chr:start-end}), and returns a numeric beta matrix suitable for region-level imprintomeR analysis such as \code{Rosenski_region}.
}
\examples{
\dontrun{
beta_region <- LoadWGBSRegionBeta("Rosenski_Adipocytes_regions_mean_beta.hg19.tsv")
}
}