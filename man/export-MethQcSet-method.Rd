% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MethQcSet-viz.R
\name{export,MethQcSet-method}
\alias{export,MethQcSet-method}
\title{Export MethQcSet Data}
\usage{
\S4method{export}{MethQcSet}(x, outdir, format = c("xlsx", "rds"), prefix = "methqcset", ...)
}
\arguments{
\item{x}{A \code{MethQcSet} object.}

\item{outdir}{Directory where files will be written.}

\item{format}{Character vector of output formats: \code{"xlsx"} (default, requires openxlsx),
\code{"rds"}, or \code{"txt"} (tab-delimited). Multiple formats can be combined.}

\item{prefix}{Character, prefix for output filenames (default: "methqcset").}

\item{...}{Additional arguments (reserved for future use).}
}
\value{
Invisibly returns a character vector of written file paths.
}
\description{
Write QC tables, beta values, and metadata to disk in various formats.
}
\details{
\strong{xlsx output (recommended):}
QC results are written into organized output locations. Root-level files include
\verb{\{prefix\}_qc_table_main.xlsx} and \verb{\{prefix\}_summary.txt}; supplementary tables go
under \code{QC_tables/}, and large data objects go under \code{data/}:
\verb{\{prefix\}_qc_table_main.xlsx} (primary QC results):
\itemize{
\item \code{metadata} sheet: Sample metadata augmented with Platform and Final.QC columns.
\item \code{QC_matrix} sheet: Per-sample QC metrics (intensity, detection p-val, coverage, sex, etc.).
\item \code{Ctrl_matrix} sheet: ewastools control metric scores (if available).
\item \code{statistics} sheet: Per-Sample_Group summary (Total, PASS, FAIL, and ratios).
}

\verb{QC_tables/\{prefix\}_qc_table_extra.xlsx} (supplementary/technical results):
\itemize{
\item \code{QC_metrics} sheet: Cutoff thresholds and decision rules (filtered to: criteria, cutoff, Final.QC).
\item \code{contamination} sheet: SNP agreement matrix for sample swap detection (if available).
\item \code{recall_rate} sheet: Per-probe detection statistics across cohort (if available).
\item \code{predUniqDonor_ID} sheet: Predicted unique donors per Sample_Group (if available).
}

Beta values are excluded from xlsx (too large); they are retained in the RDS object and optionally written as \verb{data/\{prefix\}_beta.txt} when \code{format = "txt"}.

\strong{rds output:}
Entire MethQcSet object is saved as \verb{data/\{prefix\}_qcset.rds} for programmatic access.

\strong{txt output:}
Tab-delimited beta/metadata files are written to \code{data/}; QC table text files are written to \code{QC_tables/}.
}
