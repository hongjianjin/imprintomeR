% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ImprintomeSet-summary-export.R
\name{export}
\alias{export}
\alias{export,ImprintomeSet-method}
\title{Export ImprintomeSet Outputs to Disk}
\usage{
\S4method{export}{ImprintomeSet}(
  x,
  outdir,
  result_names = NULL,
  save_plots = FALSE,
  plot_names = NULL,
  plot_device = "pdf",
  width = NULL,
  height = NULL,
  overwrite = TRUE,
  prefix = "imprintome",
  ...
)
}
\arguments{
\item{x}{An \code{ImprintomeSet} object.}

\item{outdir}{Output directory.}

\item{result_names}{Character vector of result names to export. \code{NULL} means all.}

\item{prefix}{Filename prefix for exported files, including result tables, the saved \code{ImprintomeSet} object, and plot files.}
\item{save_plots}{Logical; whether to export plot objects in \code{plots(x)}.}

\item{plot_names}{Character vector of plot names to export when \code{save_plots=TRUE}.
\code{NULL} means all stored plots.}

\item{plot_device}{Graphics device for ggplot export (\code{"pdf"} or \code{"png"}).}

\item{width}{Plot width in inches. \code{NULL} uses plot-specific defaults
(\code{12} for radar plots, otherwise \code{8}).}

\item{height}{Plot height in inches. \code{NULL} uses plot-specific defaults
(\code{12} for radar plots, otherwise \code{6}).}

\item{overwrite}{Logical; overwrite existing files.}

\item{...}{Reserved for future extensions.}
}
\value{
A data.frame manifest with one row per exported object.
}
\description{
Writes the \code{ImprintomeSet} object, selected result tables, and optionally stored plots to \code{outdir}.
Data-frame/matrix results are exported as TSV; other result objects are saved
as RDS. Plot export supports ggplot-compatible, ComplexHeatmap, recorded, and
file-backed plot objects as plot files when possible; other plot object types are serialized as RDS.
}
\examples{
\dontrun{
manifest <- export(x, outdir = "imprintome_export", prefix = "imprintome", save_plots = TRUE)
head(manifest)
}
}
