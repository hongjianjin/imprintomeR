% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_plotting.R
\name{BetaHeatmapByGene}
\alias{BetaHeatmapByGene}
\title{Heatmap of Beta Values Aggregated by Gene Symbol}
\usage{
BetaHeatmapByGene(
  beta,
  meta = NULL,
  probeset = "selected",
  SAMPLEID = "Sample_Name",
  annoColumn = "Sample_Group",
  clusterRows = TRUE,
  clusterColumns = TRUE,
  outFile = NULL,
  imgSizeFactor = 0.5
)
}
\arguments{
\item{beta}{Numeric beta matrix/data.frame with probe IDs as row names and
samples as columns.}

\item{meta}{Optional sample metadata data frame with \code{Sample_Name} and
\code{Sample_Group} columns.}

\item{probeset}{Character probeset key (default \code{"selected"}).
Must be present in \code{inst/extdata/probesets_hg19.rds}.}

\item{SAMPLEID}{Metadata column name used as sample label in heatmap
(default \code{"Sample_Name"}).}

\item{annoColumn}{Annotation column for heatmap top annotation
(default \code{"Sample_Group"}).}

\item{clusterRows}{Logical; cluster genes (rows) hierarchically
(default \code{TRUE}).}

\item{clusterColumns}{Logical; cluster samples (columns) hierarchically
(default \code{TRUE}).}

\item{outFile}{Optional output PDF file path. If provided, heatmap is saved
to disk.}

\item{imgSizeFactor}{Numeric scaling factor for saved image dimensions
(default \code{0.5}).}
}
\value{
Invisibly returns the \code{ComplexHeatmap::Heatmap} object (or NULL if
no output file and function exits silently).
}
\description{
Aggregates probe-level beta values to gene-level means by \code{Closest_TSS_gene_name}
from the selected probeset, then generates a heatmap visualization. Genes with
the same symbol are aggregated via median across probes.
}
\details{
\strong{Aggregation Logic:}
Probes are matched to the selected probeset by \code{NAME}. Probes are grouped by
their \code{Closest_TSS_gene_name} annotation. Beta values within each gene are
aggregated per sample using the median function, producing one row per unique
gene symbol.

\strong{Colors & Annotations:}
Uses a red-blue color palette spanning the observed beta range. Sample groups
are annotated at the top with automatic color assignment via \code{standardColors()}.

\strong{Clustering:}
If \code{clusterRows = TRUE}, genes are clustered via Euclidean distance and
Ward linkage. If \code{clusterRows = FALSE}, genes are sorted by their row names.
}
\examples{
\dontrun{
  # Load beta and metadata
  beta <- LoadBeta("beta.txt")
  meta <- LoadMeta("meta.txt")

  # Generate gene-level heatmap
  BetaHeatmapByGene(beta, meta, probeset = "selected",
    outFile = "gene_heatmap.pdf")
}

}
