% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ImprintomeSet-run.R
\name{runImprintome}
\alias{runImprintome}
\alias{runImprintome,ImprintomeSet-method}
\title{Run Core Imprintome Analysis}
\usage{
\S4method{runImprintome}{ImprintomeSet}(
  x,
  probeset = "selected",
  ids_cutoff = 0.2,
  result_name = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{ImprintomeSet} object.}

\item{probeset}{Character scalar probeset name (for example, \code{"selected"}).}

\item{ids_cutoff}{Numeric IDS cutoff passed to \code{AnalyzeImprintStatus()}.}

\item{result_name}{Optional name for storing output in \code{results(x)}.}

\item{...}{Reserved for future compatibility.}
}
\value{
Updated \code{ImprintomeSet} object with analysis table stored in
\code{results(x)}.
}
\description{
User-facing entry point for running the core IDS/Angle/mechanism workflow on
an \code{ImprintomeSet} object using existing \code{AnalyzeImprintStatus()} logic.
}
\examples{
\dontrun{
beta_mat <- matrix(runif(200), nrow = 20)
rownames(beta_mat) <- paste0("cg", sprintf("\%08d", seq_len(20)))
colnames(beta_mat) <- c("S1", "S2", "S3", "S4", "S5", "S6", "S7", "S8", "S9", "S10")

meta_df <- data.frame(
  Sample_Name = colnames(beta_mat),
  Sample_Group = rep(c("A", "B"), each = 5),
  stringsAsFactors = FALSE
)

probeset_df <- data.frame(
  NAME = rownames(beta_mat),
  stringsAsFactors = FALSE
)

x <- ImprintomeSet(
  beta = beta_mat,
  meta = meta_df,
  probeset = probeset_df,
  genome = "hg19",
  assay = "EPICv1"
)

x <- runImprintome(x, probeset = "selected", ids_cutoff = 0.2)
head(results(x)[["AnalyzeImprintStatus.selected"]])
}
}
