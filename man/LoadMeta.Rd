% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_io.R
\name{LoadMeta}
\alias{LoadMeta}
\title{Load and Standardize Metadata Table}
\usage{
LoadMeta(input)
}
\arguments{
\item{input}{Data frame or file path (tab-delimited with header).}
}
\value{
Standardized metadata data frame with \code{Sample_Name} row names. Columns are normalized to canonical form.
}
\description{
Accepts either a metadata data frame or path to a tab-delimited metadata file.
Validates required columns (case-insensitive) and normalizes them to canonical form.
}
\details{
\strong{Required columns (case-insensitive input; normalized to canonical form):}
\itemize{
\item \code{Sample_Name} - Unique sample identifier (normalized from ID, SAMPLE_NAME, sample_name, etc.)
\item \code{Sample_Group} - Sample grouping/stratification (normalized from GROUP, SAMPLE_GROUP, sample_group, etc.)
}

\strong{Column normalization behavior:}
User may provide columns in any case (e.g., \code{sample_name}, \code{SAMPLE_NAME}, \code{Sample_Name}).
LoadMeta() automatically normalizes to the canonical mixed-case form: \code{Sample_Name} and \code{Sample_Group}.
Preserves case of other columns.
}
